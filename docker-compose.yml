version: '3.8'

services:
  api:
    build: .
    container_name: credit-score-api
    ports:
      - "8000:8000"
    volumes:
      - ./models:/app/models  # Mount models so we don't need to rebuild to update them